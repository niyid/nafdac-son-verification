<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ir_cron_drain_regulatory_submission_queue" model="ir.cron">
        <field name="name">Drain Regulatory Submission Queue (NAFDAC/SON)</field>
        <field name="model_id" ref="model_regulatory_submission_queue"/>
        <field name="state">code</field>
        <field name="code">model._cron_drain_queue()</field>
        <field name="interval_number">30</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>

</odoo>
